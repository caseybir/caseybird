# caseybird

Static personal portfolio site for Casey Birdoff.

## Local checks

This project does not require Playwright or screenshot generation to build or verify.
Use lightweight static checks instead, such as validating that the expected tabs and
links are present in `index.html`.
